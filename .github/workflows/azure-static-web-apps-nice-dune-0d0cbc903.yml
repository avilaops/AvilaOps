name: Azure Static Web Apps CI/CD (disabled)

on:
  # Disabled duplicate workflow to avoid double deployments. Use azure-static-web-apps.yml instead.
  push:
    branches:
      - do-not-run
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - do-not-run

jobs:
  disabled_job:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use azure-static-web-apps.yml instead."
